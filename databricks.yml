bundle:
  name: databricks_bootcamp_mwua

resources:
  pipelines:
    bronze_ingestion:
      name: bronze_ingestion
      catalog: dev_mwua_catalog_team2
      schema: bronze
      photon: true
      serverless: true
      channel: CURRENT
      configuration:
        volume_base_path: /Volumes/dev_mwua_catalog_team2/landing/raw
      libraries:
        - file:
            path: ./pipelines/bronze_ingestion/
